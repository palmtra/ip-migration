---
# Placeholders only (RFC 5737). Copy this inventory and put real management
# IPs in inventories/production/hosts.yml which is gitignored.
all:
  children:
    switches:
      children:
        cisco:
          hosts:
            sw-ios-01:
              ansible_host: 192.0.2.10
              ansible_network_os: cisco.ios.ios
            sw-nxos-01:
              ansible_host: 192.0.2.11
              ansible_network_os: cisco.nxos.nxos
        arista:
          hosts:
            sw-eos-01:
              ansible_host: 192.0.2.20
              ansible_network_os: arista.eos.eos
            pe-eos-01:
              ansible_host: 192.0.2.21
              ansible_network_os: arista.eos.eos
    aggpe:
      hosts:
        pe-eos-01:
    firewalls:
      children:
        paloalto:
          hosts:
            panorama-01:
              ansible_host: 192.0.2.30
              palo_is_panorama: true
              palo_device_groups:
                - DG-SITE-A
              palo_templates:
                - TPL-NETWORK
              palo_template_stacks: []
              # Optional. Empty = discover serials from Panorama (show devices all)
              # and run ARP/FIB/live routes through Panorama targeting.
              palo_serials: []
